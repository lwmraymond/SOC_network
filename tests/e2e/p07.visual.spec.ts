import {test,expect,type Page} from '@playwright/test';
const out='docs/soc-itsm-implementation/validation/screenshots/P07';
async function ready(page:Page,url='/analyzer/search'){await page.goto(url);await expect(page.getByText('Loading query execution')).toBeHidden();}
for(const [name,width,height] of [['D1080',1920,1080],['D2K',2560,1440],['D4K',3840,2160]] as const){test(`${name} ready evidence`,async({page})=>{await page.setViewportSize({width,height});await ready(page);await page.screenshot({path:`${out}/P07-${name}-ready.png`,fullPage:true});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);});}
test('D1080 overlays and states',async({page})=>{await page.setViewportSize({width:1920,height:1080});await ready(page);await page.getByRole('button',{name:'Advanced filter builder'}).click();await page.screenshot({path:`${out}/P07-D1080-filter-builder.png`,fullPage:true});await page.keyboard.press('Escape');await page.getByRole('button',{name:/evt-/}).first().click();await page.screenshot({path:`${out}/P07-D1080-event-flyout.png`,fullPage:true});await page.keyboard.press('Escape');await page.getByRole('button',{name:'Export results'}).click();await page.screenshot({path:`${out}/P07-D1080-export-modal.png`,fullPage:true});await page.getByRole('button',{name:'Queue export'}).click();await page.screenshot({path:`${out}/P07-D1080-receipt.png`,fullPage:true});for(const [q,name] of [['network:error','error'],['denied:true','denied'],['stale:true','stale']]){await ready(page,`/analyzer/search?q=${encodeURIComponent(q)}`);await page.screenshot({path:`${out}/P07-D1080-${name}.png`,fullPage:true});}});
test('D2K flyout and density',async({page})=>{await page.setViewportSize({width:2560,height:1440});await ready(page);await page.getByRole('button',{name:/evt-/}).first().click();await page.screenshot({path:`${out}/P07-D2K-event-flyout.png`,fullPage:true});});
test('D4K wide max width',async({page})=>{await page.setViewportSize({width:3840,height:2160});await ready(page);await page.screenshot({path:`${out}/P07-D4K-wide.png`,fullPage:true});});
