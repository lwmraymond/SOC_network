# P17 — Problem Management

Status: `SOURCE_COMPLETE / SCOPED VALIDATION`

## Authority

- Route: `/itsm/problems`
- Archetype: `Problem / Known Error Workbench`
- Primary action: `Create problem`
- Fixture mode: deterministic development-only prototype

## Core task and first viewport

Recurrence clusters, RCA evidence, workaround governance and permanent-fix validation. The page separates algorithmic candidates, confirmed root cause, published knowledge and Change completion.

## Difference contract versus P16 Incidents

Problem work accumulates recurrence evidence and permanent-fix validation; Incident work restores a current service interruption.

At least three core dimensions differ: first viewport, primary workspace, primary interactions and detail/overlay flow.

## Boundaries

- Reuse existing App Shell, Router, Sidebar, Theme, PageFrame and EUI provider.
- No backend, production repository, policy engine, async worker, audit persistence or ITSM synchronization is implemented.
- Write actions create prototype-only queued receipts and never claim production completion.
- `specAligned`, `distinct` and user acceptance remain `REVIEW` / `PENDING` until visual review.
