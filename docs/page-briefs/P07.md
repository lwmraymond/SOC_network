# P07 — Event Search & Hunt

Status: `LOCAL_PASS / GITHUB SAVE PENDING`

## Authoritative inputs

- Route: `/analyzer/search`
- Archetype: `Query Workbench + Event Grid`
- Source entry: `src/pages/P07EventSearchHunt.tsx`
- Dedicated implementation: `src/p07/EventSearchPage.tsx`
- Previous delivery commit: `7068e2a414d3f5dcb164ca569b3224df2e4a2219`
- Current source blob before this checkpoint: `6cdc9c211e3e74b7314fc2dfde843b74f26763d0`

## Page contract

The page must help a threat hunter validate a hypothesis across authorized event datasets by combining a typed query, time semantics, normalized filters, exact events and a governed export path.

Primary first-viewport regions:

1. Query and time command bar.
2. Advanced filter and active-condition controls.
3. Execution summary and event histogram.
4. Cursor-backed exact event grid.
5. Event inspector, filter builder and export receipt overlays.

Primary actions:

- Run query.
- Save view/query.
- Inspect an exact event.
- Queue an export simulation.

State requirements preserved by the accepted implementation:

- loading;
- empty;
- filtered-empty;
- error;
- denied;
- stale;
- partial;
- query and URL preservation;
- field masking;
- queued is not completed.

## Differentiation contract

Closest compared surface: `P05 — Alert Queue`.

P07 differs in at least four core dimensions:

1. **First viewport:** P07 starts with a typed query/time workbench and histogram; P05 starts with alert-group triage and evidence outcome.
2. **Primary region:** P07 is an exact raw/normalized event grid; P05 is an alert-group operational queue.
3. **Primary interactions:** P07 runs, cancels, saves and exports queries; P05 assigns, classifies, links cases and evaluates suppression impact.
4. **Detail path:** P07 opens an event inspector and export job receipt; P05 opens alert-group evidence and suppression-impact decisions.

The accepted P07 source is therefore retained rather than wrapped in a generic PageFrame composition.

## Validation

- Scoped ESLint on the exact GitHub P07 entry, implementation, reducer and fixture module: `PASS`.
- Scoped strict TypeScript validation using real React/Router types and the existing foundation source: `PASS`.
- Full P07 visual direction: previously accepted by the user; this checkpoint does not redesign it.
- Production integrations remain `[BLOCKED]`.
