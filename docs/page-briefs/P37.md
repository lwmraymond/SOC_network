# P37 Page Brief

Users — Elastic-style identity governance: top-level search/filter controls, five operational identity summaries, dense user grid, effective access/security detail, sessions/activity Flyout and governed lifecycle actions.

## Difference contract

- P37 centers human identity, provider bindings, MFA, sessions and lifecycle actions; P08 centers device identity and source reconciliation.
- P37 uses user grid → effective-access detail → security action review; P38 uses role catalog → capability matrix → member impact.
- P37 detail exposes sessions, activity and ownership; P39 detail explains principal × capability × resource policy decisions.

## EUI layout review

Major modules use 24px spacing. Table content is protected by horizontal overflow wrappers, `min-width: 0`, responsive breakpoints and text wrapping. Semantic status always includes text. Visual acceptance remains `PENDING`.

Validation: `SCOPED_STRICT / SCOPED_TEMPLATE_RISK`.
