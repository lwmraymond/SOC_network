# P38 Page Brief

Roles — role catalog, capability matrix, scopes/members/diff tabs and governed publish impact. No decorative KPI or chart is used.

## Difference contract

- P38 defines role capabilities, scopes, members and version impact; P37 manages individual identity, sessions and MFA.
- P38 uses catalog → matrix/editor → member impact; P39 uses principal × capability × resource evaluation → grant records → decision trace.
- Publication models privilege union, SoD conflicts and rollback; it does not claim immediate effective access.

## EUI layout review

Major modules use 24px spacing, responsive grid collapse, wrapped text and horizontally protected matrices. Visual acceptance remains `PENDING`.

Validation: `SCOPED_STRICT / SCOPED_TEMPLATE_RISK`.
