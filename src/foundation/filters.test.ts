import {describe,it,expect} from 'vitest';import {addCondition,applyFieldPermissions,dependencyReason,deserializeFilters,emptyFilters,normalizeFlatFilters,removeFilter,serializeFilters,updateCondition} from './filters';
const c={id:'1',kind:'condition' as const,field:'severity',operator:':' as const,value:'high',valueType:'string' as const,exclude:false,label:'high'};describe('filters',()=>{it('adds updates removes',()=>{let f=addCondition(emptyFilters(),c);f=updateCondition(f,'1',{exclude:true});expect((f.children[0] as typeof c).exclude).toBe(true);expect(removeFilter(f,'1').children).toHaveLength(0)});it('groups same field OR and different AND',()=>{const f=normalizeFlatFilters([c,{...c,id:'2',value:'critical'},{...c,id:'3',field:'source'}]);expect(f.operator).toBe('AND');expect(f.children[0].kind).toBe('group')});it('round trips and masks',()=>{const f=addCondition(emptyFilters(),c);expect(deserializeFilters(serializeFilters(f))).toEqual(f);expect(applyFieldPermissions(f,[{name:'severity',label:'Severity',type:'string',operators:[':'],visible:false}]).warnings).toHaveLength(1)});it('dependency reason',()=>expect(dependencyReason({name:'city',label:'City',type:'string',operators:[':'],visible:true,dependsOn:'country'},emptyFilters())).toMatch('country'))});
