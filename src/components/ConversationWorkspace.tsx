import { useState } from 'react';
import { EuiBadge, EuiButton, EuiFieldText, EuiPanel, EuiTitle } from '@elastic/eui';
export function ConversationWorkspace() {
  const [message,setMessage]=useState('Explain the evidence and cite the attached case.');
  return <div className="conversationWorkspace" data-visual-region="conversation"><aside><EuiTitle size="xs"><h2>Sessions</h2></EuiTitle>{['High-risk login review','Change impact summary','Asset exposure context','Queue handoff'].map((item,index)=><button key={item} type="button"><span>{item}</span><small>{index+1} context objects</small></button>)}</aside><EuiPanel paddingSize="m" className="conversationThread"><div className="message user"><strong>Analyst</strong><p>Summarize why the activity is risky and identify uncertainty.</p></div><div className="message assistant"><strong>Copilot prototype</strong><p>The answer is scoped to explicitly attached fixture objects. Citations, ACL, revision and freshness are shown before any tool proposal.</p><div><EuiBadge color="hollow">Citation 1 · Case</EuiBadge><EuiBadge color="hollow">Citation 2 · Asset</EuiBadge></div></div><div className="composer"><EuiFieldText aria-label="Copilot message" value={message} onChange={(event)=>setMessage(event.target.value)} /><EuiButton fill>Send prototype message</EuiButton></div></EuiPanel><EuiPanel paddingSize="m"><EuiTitle size="xs"><h2>Evidence context</h2></EuiTitle><p>2 objects · 1 stale citation · classification confidential</p><EuiBadge color="warning">No tool execution</EuiBadge></EuiPanel></div>;
}
