import { useState } from 'react';
import { EuiBadge, EuiButton, EuiFieldNumber, EuiPanel, EuiSwitch, EuiTitle } from '@elastic/eui';
export function ThemeWorkbench() {
  const [radius,setRadius]=useState(8);const[dark,setDark]=useState(false);const[focus,setFocus]=useState(true);
  return <div data-visual-region="theme-workbench" className={`themeWorkbench ${dark?'themePreviewDark':''}`}><aside><EuiTitle size="xs"><h2>Token groups</h2></EuiTitle>{['Color semantics','Typography','Spacing','Radius','Focus','Motion','Data density'].map((item)=><button key={item} type="button">{item}</button>)}</aside><EuiPanel paddingSize="m"><EuiTitle size="s"><h2>Live token preview</h2></EuiTitle><div className="tokenControls"><label>Radius<EuiFieldNumber value={radius} min={0} max={24} onChange={(event)=>setRadius(Number(event.target.value))}/></label><EuiSwitch label="Dark preview" checked={dark} onChange={(event)=>setDark(event.target.checked)}/><EuiSwitch label="Visible keyboard focus" checked={focus} onChange={(event)=>setFocus(event.target.checked)}/></div><div className="themePreview" style={{borderRadius:radius}}><header><strong>Platform preview</strong><EuiBadge color="success">Ready</EuiBadge></header><button className={focus?'focusDemo':''}>Primary action</button><table><tbody><tr><th>Status</th><td>Non-color text label</td></tr><tr><th>Contrast</th><td>Automated validation pending</td></tr></tbody></table></div><div className="settingsActions"><EuiButton>Run accessibility validation</EuiButton><EuiButton fill>Publish prototype revision</EuiButton></div></EuiPanel></div>;
}
