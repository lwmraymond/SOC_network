import {useEffect,useMemo,useReducer,useRef,useState} from 'react'; import {useSearchParams} from 'react-router-dom'; import {EuiBadge,EuiBasicTable,EuiButton,EuiButtonEmpty,EuiCallOut,EuiFieldSearch,EuiFlexGroup,EuiFlexItem,EuiFlyout,EuiFlyoutBody,EuiFlyoutHeader,EuiModal,EuiModalBody,EuiModalFooter,EuiModalHeader,EuiModalHeaderTitle,EuiPageTemplate,EuiPanel,EuiSelect,EuiSpacer,EuiStat,EuiText,EuiTitle} from '@elastic/eui';
import {createEnvelope,parseQuery,validateQuery,type FieldDefinition} from '../foundation/query'; import {emptyFilters,type FilterCondition,addCondition} from '../foundation/filters'; import {FixtureCursorAdapter} from '../foundation/cursor'; import {PrototypePermissionService,safeCount} from '../foundation/permissions'; import {PrototypeExportJobService,type ExportJob} from '../foundation/actions'; import {InMemorySavedViewRepository} from '../foundation/savedViews'; import {prototypeEvents,type EventRecord,isPrototypeMode} from '../prototype/events'; import {initialP07State,p07Reducer} from './store';
const adapter=new FixtureCursorAdapter(prototypeEvents); const policy=new PrototypePermissionService(); const exportsService=new PrototypeExportJobService(); const savedViews=new InMemorySavedViewRepository(); void savedViews;
const catalog:FieldDefinition[]=[{name:'severity',operators:[':', '=','!='],visible:true,type:'keyword'},{name:'source',operators:[':', '=','PREFIX'],visible:true,type:'keyword'},{name:'event_time',operators:['>','>=','<','<='],visible:true,type:'date'},{name:'user.email',operators:[':', '='],visible:false,type:'string'}];
export function EventSearchPage(){const[params,setParams]=useSearchParams();const[state,dispatch]=useReducer(p07Reducer,{...initialP07State,query:params.get('q')??'',status:(params.get('state') as typeof initialP07State.status)||'ready'});const[draft,setDraft]=useState(state.query);const[rows,setRows]=useState<EventRecord[]>([]);const[next,setNext]=useState<string>();const[prev,setPrev]=useState<string>();const[filters,setFilters]=useState(emptyFilters());const[errors,setErrors]=useState<ReturnType<typeof validateQuery>>([]);const[selected,setSelected]=useState<EventRecord>();const[job,setJob]=useState<ExportJob>();const opener=useRef<HTMLButtonElement|null>(null);
const envelope=useMemo(()=>createEnvelope(state.query,{cursor:state.cursor}),[state.query,state.cursor]);
useEffect(()=>{if(!isPrototypeMode)return;const controller=new AbortController();dispatch({type:'status',value:'loading'});adapter.search({query:envelope,filters,cursor:state.cursor,limit:12,sort:envelope.sort,signal:controller.signal}).then(async r=>{if(r.error){dispatch({type:'status',value:r.error.classification==='DENIED'?'denied':'error'});setRows([]);return;}const visible:EventRecord[]=[];for(const row of r.items){const d=await policy.decide({subject:'analyst-01',tenant:'demo',scope:['security'],kind:'row',capability:'events.read',objectType:'event',objectId:row.id});if(d.allowed)visible.push(row);}setRows(visible);setNext(r.nextCursor);setPrev(r.previousCursor);dispatch({type:'status',value:r.partial?'partial':r.stale?'stale':visible.length?'ready':state.query?'filtered-empty':'empty'});});return()=>controller.abort();},[envelope,filters,state.cursor,state.query]);
const run=()=>{const parsed=parseQuery(draft);const e=[...parsed.errors,...validateQuery(parsed.ast,catalog,envelope.timeRange)];setErrors(e);if(e.length)return;dispatch({type:'query',value:draft});const p=new URLSearchParams(params);if(draft)p.set('q',draft);else p.delete('q');setParams(p);};
const addHigh=()=>setFilters(f=>addCondition(f,{id:crypto.randomUUID(),kind:'condition',field:'severity',operator:':',value:'high',valueType:'string',exclude:false,label:'Severity is high'} as FilterCondition));
const exportNow=async()=>{const decision=await policy.decide({subject:'analyst-01',tenant:'demo',scope:['security'],kind:'export',capability:'events.export',objectType:'query'});const j=await exportsService.create({classification:'confidential',fields:envelope.projection,filters,query:envelope,timeRange:envelope.timeRange,rowCountEstimate:rows.length,permissionDecision:decision,redactions:['user.email']},'analyst-01');setJob(j);dispatch({type:'showReceipt'});};
const statusCallout=state.status!=='ready'&&state.status!=='loading'?<EuiCallOut title={state.status.replace('-',' ')} color={state.status==='error'||state.status==='denied'?'danger':'warning'}>Query state is structured and query, filters and URL are preserved.</EuiCallOut>:null;
const display=state.status==='empty'||state.status==='filtered-empty'?[]:rows;const selectedDecision=selected?.restricted?'masked':'visible';
return <EuiPageTemplate panelled restrictWidth={1800}><EuiPageTemplate.Header pageTitle="Event Search & Hunt" description="P07 · normalized query workbench, cursor grid and governed export" rightSideItems={[<EuiButton key="save" onClick={()=>void 0}>Save view</EuiButton>]}/><EuiPageTemplate.Section><EuiCallOut title="Prototype Mode · Fixture Data · No production mutations" color="warning">All adapters are replaceable interfaces. Production has no fixture fallback.</EuiCallOut><EuiSpacer size="m"/><EuiFlexGroup gutterSize="s" alignItems="flexEnd" responsive={false} className="queryRow"><EuiFlexItem grow={2}><EuiFieldSearch value={draft} onChange={e=>setDraft(e.target.value)} onSearch={run} aria-label="Event query" placeholder={'severity:high AND source:edge-firewall'}/></EuiFlexItem><EuiFlexItem grow={false}><EuiSelect aria-label="Time field" options={[{value:'event_time',text:'Event time'},{value:'ingested_at',text:'Ingested time'}]}/></EuiFlexItem><EuiFlexItem grow={false}><EuiButton fill onClick={run}>Run query</EuiButton></EuiFlexItem></EuiFlexGroup>{errors.length>0&&<><EuiSpacer size="s"/><EuiCallOut title="Query parse error" color="danger">{errors.map(e=><p key={`${e.code}-${e.span.start}`}><strong>{e.token}</strong>: {e.message} {e.recoverHint}</p>)}</EuiCallOut></>}<EuiSpacer size="s"/><EuiFlexGroup gutterSize="s"><EuiFlexItem grow={false}><EuiButtonEmpty buttonRef={opener} onClick={()=>dispatch({type:'toggleFilter'})}>Advanced filter builder</EuiButtonEmpty></EuiFlexItem>{filters.children.map(c=><EuiBadge key={c.id}>{c.kind==='condition'?c.label:'Filter group'}</EuiBadge>)}<EuiFlexItem/><EuiFlexItem grow={false}><EuiButtonEmpty onClick={()=>dispatch({type:'toggleExport'})}>Export results</EuiButtonEmpty></EuiFlexItem></EuiFlexGroup><EuiSpacer/><EuiPanel><EuiFlexGroup><EuiFlexItem><EuiStat title={safeCount(display.length,{decisionId:'local',allowed:true,visible:true,reasonCode:'ALLOWED',reason:'',policySource:'',freshness:'',audit:{subject:'',capability:'',scope:[]}})??'—'} description="Visible results"/></EuiFlexItem><EuiFlexItem><EuiStat title="100%" description="Coverage"/></EuiFlexItem><EuiFlexItem><EuiStat title="8s" description="Freshness lag"/></EuiFlexItem><EuiFlexItem><EuiStat title="Asia/Taipei" description="Timezone"/></EuiFlexItem></EuiFlexGroup><div className="histogram" role="img" aria-label="Event histogram. Exact table follows.">{[30,55,42,75,60,88,45,65,35,70,52,80].map((h,i)=><span key={i} style={{height:`${h}%`}}/>)}</div><table className="chartFallback"><caption>Histogram exact-data fallback</caption><tbody><tr><th>Visible events</th><td>{display.length}</td></tr></tbody></table></EuiPanel><EuiSpacer/>{statusCallout}{state.status==='loading'?<EuiCallOut title="Loading query execution">Cancellation is wired through AbortSignal.</EuiCallOut>:<EuiBasicTable tableCaption="Event search results" items={display} itemId="id" columns={[{field:'id',name:'Event ID',render:(v:string,item:EventRecord)=><EuiButtonEmpty size="xs" onClick={()=>{setSelected(item);dispatch({type:'select',value:item.id});}}>{v}</EuiButtonEmpty>},{field:'event_time',name:'Event time',sortable:true},{field:'severity',name:'Severity',render:(v:string)=><EuiBadge>{v}</EuiBadge>},{field:'source',name:'Source'},{field:'host',name:'Host'},{field:'user',name:'User',render:(v:string,item:EventRecord)=>item.restricted?<span aria-label="masked field">••••••</span>:v},{field:'action',name:'Action'}]} noItemsMessage={state.status==='filtered-empty'?'No events match filters.':'No events available.'}/>}<EuiSpacer size="s"/><EuiFlexGroup justifyContent="spaceBetween"><EuiButtonEmpty isDisabled={!prev} onClick={()=>dispatch({type:'cursor',value:prev})}>Previous</EuiButtonEmpty><EuiButtonEmpty isDisabled={!next} onClick={()=>dispatch({type:'cursor',value:next})}>Next</EuiButtonEmpty></EuiFlexGroup></EuiPageTemplate.Section>{state.filterOpen&&<EuiFlyout onClose={()=>{dispatch({type:'toggleFilter'});opener.current?.focus();}} ownFocus size="s" aria-labelledby="filter-title"><EuiFlyoutHeader><EuiTitle><h2 id="filter-title">Advanced filter builder</h2></EuiTitle></EuiFlyoutHeader><EuiFlyoutBody><EuiText><p>Filter chips and builder share the same nested model. Stale facets never clear input.</p></EuiText><EuiButton onClick={addHigh}>Add severity:high</EuiButton></EuiFlyoutBody></EuiFlyout>}{selected&&<EuiFlyout onClose={()=>setSelected(undefined)} ownFocus size="m" aria-labelledby="inspector-title"><EuiFlyoutHeader><EuiTitle><h2 id="inspector-title">Event inspector</h2></EuiTitle></EuiFlyoutHeader><EuiFlyoutBody><EuiCallOut title={selectedDecision==='masked'?'Field masking applied':'Authorized fields'} iconType="lock">Route, row, field, action and export use one policy interface.</EuiCallOut><EuiSpacer/><dl className="detailGrid"><dt>Event ID</dt><dd>{selected.id}</dd><dt>Event time</dt><dd>{selected.event_time}</dd><dt>Ingested</dt><dd>{selected.ingested_at}</dd><dt>Source</dt><dd>{selected.source}</dd><dt>User</dt><dd>{selected.restricted?'••••••':selected.user}</dd><dt>Message</dt><dd>{selected.message}</dd></dl></EuiFlyoutBody></EuiFlyout>}{state.exportOpen&&<EuiModal onClose={()=>dispatch({type:'toggleExport'})} aria-labelledby="export-title"><EuiModalHeader><EuiModalHeaderTitle id="export-title">Create export job</EuiModalHeaderTitle></EuiModalHeader><EuiModalBody><EuiCallOut title="Prototype simulation" color="warning">No production API is called. Queued does not mean completed.</EuiCallOut><EuiText><p>Classification: Confidential · masked fields: user.email · rows estimated: {rows.length}</p></EuiText></EuiModalBody><EuiModalFooter><EuiButtonEmpty onClick={()=>dispatch({type:'toggleExport'})}>Cancel</EuiButtonEmpty><EuiButton fill onClick={exportNow}>Queue export</EuiButton></EuiModalFooter></EuiModal>}{state.receiptOpen&&job&&<EuiFlyout onClose={()=>dispatch({type:'showReceipt'})} ownFocus aria-labelledby="receipt-title"><EuiFlyoutHeader><EuiTitle><h2 id="receipt-title">Action receipt</h2></EuiTitle></EuiFlyoutHeader><EuiFlyoutBody><EuiCallOut title="Prototype simulation · queued" color="warning">Accepted and queued are not completed. Authoritative rehydration is pending.</EuiCallOut><EuiText><p>Receipt: {job.receipt.receiptId}</p><p>Request: {job.receipt.requestId}</p><p>Status: {job.receipt.status}</p></EuiText></EuiFlyoutBody></EuiFlyout>}</EuiPageTemplate>}
